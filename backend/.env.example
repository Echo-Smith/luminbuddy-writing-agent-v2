# Writing Agent V2 — Environment Variables

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=120s

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/writing_agent_v2?sslmode=disable

# Optional: absolute path to the redacted private Holdout JSONL used only by WABench replay.
# The importer stores references and hashes; it does not copy private inputs into PostgreSQL.
WABENCH_PRIVATE_INPUT_JSONL=
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# Redis (optional)
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET=change-this-in-production
JWT_EXPIRY=24h

# Admin
ADMIN_TOKEN=change-this-in-production

# DeepSeek LLM
DEEPSEEK_BASE_URL=https://api.deepseek.com
AI_API_KEY=your-deepseek-api-key-here
DEEPSEEK_DEFAULT_MODEL=deepseek-v4-flash
DEEPSEEK_TIMEOUT=120s
DEEPSEEK_MAX_TOKENS=131072
DEEPSEEK_TEMPERATURE=0.7
# A/B test ratio for Responses API (0.0=off, 0.5=50/50, 1.0=full Responses API)
DEEPSEEK_RESPONSES_API_RATIO=0

# Dashscope (Embedding)
DASHSCOPE_API_KEY=your-dashscope-api-key
DASHSCOPE_MODEL=text-embedding-v3
DASHSCOPE_DIMENSION=1024

# Provider readiness probes are opt-in because they use credentials and some
# providers bill per request. Admins can always run POST /api/v2/admin/provider-preflight.
PROVIDER_PREFLIGHT_ENABLED=false
PROVIDER_PREFLIGHT_INTERVAL=30m
PROVIDER_PREFLIGHT_TIMEOUT=8s

# MCP extension points (OSS shares the protocol/runtime, not commercial source credentials)
MCP_SERVERS=[]
MCP_SERVER_ENABLED=false
MCP_SERVER_HTTP_ADDR=:9090
MCP_SERVER_STDIO=false

# Paid search provider variables intentionally do not exist in the OSS example.

# Knowledge Base (local — BM25 + Dense + GraphRAG on PostgreSQL)
DOCREADER_ADDR=docreader:50051
DOCREADER_TRANSPORT=tcp
KB_CHUNK_SIZE=512
KB_CHUNK_OVERLAP=50
KB_BM25_WEIGHT=0.3
KB_DENSE_WEIGHT=0.5
KB_GRAPH_WEIGHT=0.2

# LLM (for GraphRAG entity extraction — reuses DeepSeek API)
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=your-deepseek-api-key
LLM_MODEL=deepseek-v4-flash

# WebAuthn / Passkeys
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=笔润智谈
WEBAUTHN_RP_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Agent
AGENT_MODE=unified
AGENT_TIMEOUT=5m
AGENT_MAX_TOKENS=300000
AGENT_MAX_FIX_ATTEMPTS=2
AGENT_MAX_CONCURRENT=10
AGENT_MAX_CONCURRENT_PER_USER=1
AGENT_CONFIRM_TIMEOUT=5m
AGENT_CIRCUIT_BREAKER_FAILS=3
