/**
 * ─── 动画体系 Barrel Export ─────────────────────────────────
 *
 * 统一入口，按需引用：
 *   import { useInView, presets, DURATION } from "@/lib/animation";
 */
export * from "./tokens";
export * from "./hooks";
export * from "./presets";
